# GenLayer Intelligent Contract Specification

GenLayer allows contracts to interact with non-deterministic off-chain data and execute natural language prompts.

## Key APIs
- `gl.get_web_data(url, headers)`: Live validator HTTP health probe.
- `gl.exec_prompt(prompt, criteria)`: Subjective dispute arbitration by validator jury.
